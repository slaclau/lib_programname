name: Build debian

on:
  push:
  
jobs:
  build:
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        os: [ubuntu-22.04, ubuntu-20.04]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Get build dependencies
        run: |
          sudo apt-add-repository ppa:slaclau/ppa-pre
          sudo apt update
          sudo apt install devscripts equivs
          sudo mk-build-deps -i -t "apt-get -o Debug::pkgProblemResolver=yes --no-install-recommends -y"
      - name: Import key
        run: |
          GPG_KEY_ID=$(echo "${{ secrets.PPA_GPG_PRIVATE_KEY }}" | gpg --import-options show-only --import | sed -n '2s/^\s*//p')
          echo $GPG_KEY_ID
          echo "${{ secrets.PPA_GPG_PRIVATE_KEY }}" | gpg --batch --import
      - name: Build
        run: |
          python3 updateChangelog.py 
          dpkg-buildpackage -S -sa
          dpkg-buildpackage -b
          mv ../python-lib-programname_*.* .
      - name: Upload debian package
        uses: actions/upload-artifact@v3
        with:
          name: ${{ matrix.os }}
          path: |
            python-lib-programname*.deb
            python-lib-programname*.dsc
            python-lib-programname*.tar.xz
            python-lib-programname*.changes
      - name: Publish package to prerelease PPA
        run: |
          sudo apt install dput
          dput ppa:slaclau/ppa-pre python-lib-programname*source.changes
        if: github.ref == 'refs/heads/master'